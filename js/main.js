document.addEventListener('DOMContentLoaded', () => {
    // Basic interaction tracking or effects could go here
    console.log("Saddl Links Loaded");

    // Optional: Add simple ripple effect or advanced hover logic if requested later
});
